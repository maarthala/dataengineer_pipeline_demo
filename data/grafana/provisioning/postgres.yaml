apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    isDefault: true
    editable: true
    url: postgres:5432
    database: northwind
    user: airflow
    secureJsonData:
      password: airflow
    jsonData:
      sslmode: disable
